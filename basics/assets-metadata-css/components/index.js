export { Layout, siteTitle } from "./layout";
export { Alert } from "./alert";

// If want to more be catagorize
/* import { Layout, siteTitle } from "./layout";
import { Alert } from "./alert";

const component = {
  Layout,
  Alert
}
const additional = {
  siteTitle
}

export { component }
export { additional } */
